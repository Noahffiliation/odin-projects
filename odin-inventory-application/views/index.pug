extends layout

block content
  h1= title
  p Welcome to #[em Game Inventory]

  h1 Dynamic content

  if error
    p Error getting dynamic content.
  else
    p The inventory has the following record counts:

    ul
      li #[strong Games:] !{data.game_count}
      li #[strong Genres:] !{data.genre_count}
